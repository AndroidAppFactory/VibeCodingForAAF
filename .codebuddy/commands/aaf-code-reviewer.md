# 代码审查（CR）

对指定文件或当前打开的文件执行 CR 级深度代码审查。

## 执行规则

**必须调用 `aaf-code-reviewer` Agent 执行**，按该 Agent 定义的完整检查规则体系运行。

补充要求：
1. 确定审查范围：优先使用用户指定的文件/目录，否则使用当前打开的文件
2. 按严重级别输出报告（P0 Crash > P1 内存泄露 > P2 性能 > P3 架构 > P4 建议）
3. 对可自动修复的问题，询问用户是否执行
